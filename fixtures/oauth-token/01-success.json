{
  "name": "Success, token issued",
  "description": "Standard OAuth 2.0 client_credentials grant response. The cartridge should cache this token until `issued_at + expires_in - 60s`.",
  "request": {
    "body": {
      "grant_type": "client_credentials",
      "client_id": "<connected-app-consumer-key>",
      "client_secret": "<connected-app-consumer-secret>"
    }
  },
  "response": {
    "status": 200,
    "body": {
      "access_token": "00D5g000004uXXX!AQEAQI.MOCK_TOKEN_DO_NOT_USE_IN_PROD",
      "instance_url": "https://example.my.salesforce.com",
      "id": "https://login.salesforce.com/id/00D5g000004uXXX/0055g00000XYZ",
      "token_type": "Bearer",
      "issued_at": "1700000000000",
      "expires_in": 3600,
      "signature": "MOCK_SIGNATURE"
    }
  },
  "notes": "Real responses do not include `expires_in`, the cartridge has to assume a default (e.g. 2h). Confirm with the SF admin which connected-app session policy is configured."
}
